# =============================================================================
# Vector Store Configuration
# =============================================================================

vector_store:
  persist_directory: data/chroma
  collection_name: amazon_products
  validation_collection_name: fashion_products_validation
  distance_metric: cosine
  allow_reset: true